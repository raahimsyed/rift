{
  "name": "rift-proxy",
  "version": "1.0.0",
  "description": "Rift unblocked proxy",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "validate:vercel": "node -e \"const fs=require('fs');const v=JSON.parse(fs.readFileSync('vercel.json','utf8'));if(v?.rewrites?.[0]?.destination!=='/api/index'){throw new Error('vercel.json must rewrite to /api/index')}console.log('vercel.json ok')\""
  },
  "dependencies": {
    "express": "^4.22.1",
    "@mercuryworkshop/bare-mux": "^2.1.6",
    "@mercuryworkshop/libcurl-transport": "^1.3.1",
    "@mercuryworkshop/scramjet": "^1.0.2",
    "@mercuryworkshop/wisp-js": "^0.4.1"
  }
}
